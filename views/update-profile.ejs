<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Profile</title>
</head>
<body class="mx-4">

    
        <form action="" method="post">
          <div class="mb-3">
            <label for="" class="form-label">Full Name</label>
            <input type="text" class="form-control" name="fullName" value="<%= profile.fullName %>">
          </div>
          <div class="mb-3">
            <label for="" class="form-label">Gender</label>
            <select class="form-select" aria-label="Default select example" name="gender" value="<%= profile.gender %>">
                <option selected>--Select Gender--</option>
                <option value="M" <%= profile.gender == "M" ? 'selected' : ''%>>Male</option>
                <option value="F" <%= profile.gender == "F" ? 'selected' : ''%>>Female</option>
            </select>
          </div>
          <div class="mb-3">
            <label for="" class="form-label">Birth Date</label>
            <input type="date" class="form-control" name="dateOfBirth" value="<%= profile.getFormatDate(profile.dateOfBirth) %>">
          </div>
          <div class="mb-3">
            <label for="" class="form-label">Phone Number</label>
            <input type="text" class="form-control" name="phone" value="<%= profile.phone %>">
          </div>
          <a href="/profile"><button>Cancel</button></a>
          <button type="submit" class="btn btn-primary">Save</button>
        </form>
      
        
</body>
</html>